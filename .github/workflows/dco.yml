name: DCO

# Thin caller: the actual check lives in the org-shared reusable workflow
# at agentnameservice/.github — fix or extend the policy there, not here.

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  dco:
    name: DCO
    uses: agentnameservice/.github/.github/workflows/dco-check.yml@main
